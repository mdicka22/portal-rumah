const CACHE_NAME='portal-rumah-v10';
const APP_SHELL=['./','./index.html','./app.js','./styles.css','./document-preview.css','./login-blend.css','./ios-pwa.css','./assets/login-interior-coral.png','./assets/apple-touch-icon.png'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE_NAME).then(cache=>cache.addAll(APP_SHELL)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE_NAME).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET'||new URL(event.request.url).origin!==self.location.origin)return;event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE_NAME).then(cache=>cache.put(event.request,copy));return response}).catch(()=>caches.match(event.request).then(cached=>cached||caches.match('./index.html'))))});
self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={title:'Portal Rumah',body:event.data?event.data.text():''}}const title=data.title||'Portal Rumah';event.waitUntil(self.registration.showNotification(title,{body:data.body||'Ada kabar baru dari keluarga',icon:'./assets/apple-touch-icon.png',badge:'./assets/apple-touch-icon.png',tag:data.tag||'portal-rumah',renotify:true,data:{url:data.url||'./'},actions:[{action:'open',title:'Buka'}]}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const target=new URL(event.notification.data?.url||'./',self.location.origin).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(async list=>{for(const client of list){if('navigate' in client)await client.navigate(target);if('focus' in client)return client.focus()}if(clients.openWindow)return clients.openWindow(target)}))});
